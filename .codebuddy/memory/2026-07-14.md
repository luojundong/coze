# 2026-07-14 工作日志

## 构建打包 v49
- 修改文件：`src/app/login/page.tsx`（分销码绑定改为注册+登录都绑）、`src/app/api/wechat/wxacode/route.ts`（增加诊断日志）、`miniprogram/pages/login/login.js`（新增 onShow 检查 pendingRef）
- 执行 `bash ./scripts/build.sh`（Next.js build + tsup 打包 server）
- 打包 `coze-deploy-v49.zip`（7.42 MB）
- 部署命令：`rm -rf .next dist && unzip -o coze-deploy-v49.zip && pm2 reload cozeZnt`
- 小程序端 login.js 改动需单独在微信开发者工具中重新上传

## 修复：分销二维码生成 — wxacode 自动降级（v50）
- 用户反馈正式环境下分销海报的二维码扫码无效，原因是个人小程序不支持配置「业务域名」，网页链接二维码被微信拦截。
- 将 `/api/wechat/wxacode` 改为 `envVersion` 自动降级策略：release → trial → develop，任一成功即返回。
- 新增 `requestWxacode()` 函数封装单次请求，修复了原代码中未传 `access_token` 到微信 API 的 bug。
- 打包 `coze-deploy-v50.zip`（7.42 MB）
- 部署后若仍失败，需要排查：小程序是否已上传代码、提交审核、审核通过后点击发布。

## 修复：workflow 工具输出显示 + 对话记忆（v56）
- 用户反馈小程序端使用工具生成的链接又出现 TOS 签名 URL 原文，且工具对话框没有之前记忆。
- 修复点 1（`miniprogram/pages/tool-detail/tool-detail.js`）：
  - `handleRunWorkflow` 中新增 `extractWorkflowOutput()` 提取 `res.output`，避免 `JSON.stringify(res, null, 2)` 把响应对象整段当文本显示。
  - 支持 output 为嵌套 JSON 字符串时自动提取 `url/link/file_url/output/content` 字段。
  - `handleRunWorkflow` 现在会先把用户输入写入 `messages` 并清空输入框，再追加 assistant 回复，保证历史完整。
- 修复点 2（`miniprogram/pages/tool-detail/tool-detail.js`）：`onUnload` 先调用 `_saveChatHistory()` 再清理定时器，防止运行中退出导致历史丢失。
- 修复点 3（后端）：`/api/workflow/stream`、`/api/workflow/run`、`/api/workflow/task/run` 中，只要前端提供了 `conversation_id` 就始终传给 Coze，不再要求 `truncatedHistory.length > 0`，确保 bot 多轮上下文连续。
- 执行 `pnpm next build` + `pnpm tsup` + `package-v27-style.ps1`，打包 `coze-deploy-v56.zip`（7.44 MB）。
- 小程序端改动需在微信开发者工具中重新编译上传。
